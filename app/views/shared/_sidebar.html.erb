<aside class="sidebar">
  <ul>
    <li>
      <a href="#"
         data-controller="sidebar"
         data-sidebar-url="<%= overview_dashboards_path(current_user) %>"
         data-action="click->sidebar#loadContent"
         class="btn btn-primary">Overview
      </a>
    </li>
    <li>
      <a href="#"
         data-controller="sidebar"
         data-sidebar-url="<%= exercise_plan_dashboards_path(current_user) %>"
         data-action="click->sidebar#loadContent"
         class="btn btn-primary">Exercise Plans
      </a>
    </li>
    <li>
      <a href="#"
         data-controller="sidebar"
         data-sidebar-url="<%= diet_plan_dashboards_path(current_user) %>"
         data-action="click->sidebar#loadContent"
         class="btn btn-primary">Diet Plans
      </a>
    </li>
    <li>
      <a href="#"
         data-controller="sidebar"
         data-sidebar-url="<%= videos_path(current_user) %>"
         data-action="click->sidebar#loadContent"
         class="btn btn-primary">Videos
      </a>
    </li>
    <li>
      <a href="#"
         data-controller="sidebar"
         data-sidebar-url="<%= overview_dashboards_path(current_user) %>"
         data-action="click->sidebar#loadContent"
         class="btn btn-primary">Progress
      </a>
    </li>
  </ul>
</aside>
